# R
.Rhistory
.RData
.Rproj.user/
*.Rproj
.Ruserdata

# Data (raw/processed outputs -- keep small ref files by exempting them below)
data/raw/
data/processed/
data/*.rds
!data/marker_genes.rds
!data/pemt_signature.rds

# Figures generated by scripts (keep the one referenced in README -- must be
# `figures/*`, not `figures/`, or git prunes the directory before the
# negation below ever gets a chance to apply)
figures/*
!figures/soft_vs_hard_top_edges.png
!figures/pemt_entropy_correlation.png

# OmnipathR's local cache/log directory (created when running LIANA)
omnipathr-log/

# R plotting device artifact (created when ggplot2 runs non-interactively)
Rplots.pdf

# OS
.DS_Store

# Claude Code local session config
.claude/
